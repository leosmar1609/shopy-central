# Carregado automaticamente pelo Vite quando roda "npm run build" (modo production).
# Sobrescreve só o necessário do .env local — o resto (segredos) vem dos
# "wrangler secret put" já configurados no Worker, não daqui.
VITE_APP_URL=https://sshswift.com.br
